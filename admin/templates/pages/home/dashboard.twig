{% extends 'layout.twig' %}

{% block body %}
<!-- Breadcrumbs-->
{% include "components/breadcrumb.twig" %}
<!-- Icon Cards-->
<div class="row">
  {% import "components/icon_card.twig" as icon %}
  {{ icon.card('primary', 'fas fa-fw fa-comments', 'Files Uploaded','View more', '#', {dynamic: dashboard.files})}}
  {{ icon.card('warning', 'fas fa-fw fa-list', 'Active users','View more', '#')}}
  {{ icon.card('success', 'fas fa-fw fa-shopping-cart', 'Posts created','View more', '#')}}
  {{ icon.card('danger', 'fas fa-fw fa-life-ring', 'Comments','View more', '#')}}
</div>
<!-- Area Chart Example-->
<div class="card mb-3">
              <div class="card-header">
                <i class="fas fa-info-circle"></i> Plans for future, {{navbar.username}}</div>
              <div class="card-body">
                <div class="col-12">
                  <div class="row">
                    <div class="col-6">
                      <h4>To be done:</h4>
                      <ul>
                        <li>Manage Images & Videos (FileSystem)</li>
                        <li>Posts (TextSystem)</li>
                        <li>Manage content generated by users (ManagementSystem)</li>
                        <li>Pretty urls</li>
                        <li>User interactions & communication (SocialSystem)</li>
                        <li>Working with data (StatisticsSystem</li>
                        <li>Handling errors and requests (Logger)</li>
                      </ul>
                    </div>
                    <div class="col-6">
                      <h4>Known bugs:</h4>
                      <ul>
                        <li>A valid token can be used anywhere, no matter of it's scope (<0.2)</li>
                        <li>Emails lose their style (<0.3)</li>
                        <li>You can't install the application if you are not logged in, and you can't login because there is no database. (Only for 0.3)</li>
                        <li>Session cookies are not encrypted. (<0.4)</li>
                        <li>Remote code execution in `Upload Files` page. (<0.4)</li>
                        <li>Tokens are the same as UUID, not generated securely. (<0.4)</li>
                      </ul>
                    </div>
                  </div>
                </div>
{% endblock %}
