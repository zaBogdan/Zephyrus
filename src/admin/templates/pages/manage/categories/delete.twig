{% extends 'layout.twig' %}

{% block body %}

{% include "components/breadcrumb.twig" %}
{% import 'components/forms.twig' as forms %}
{% set response = deletePost(post) %}

<center><h3>This function is not yet implemented! Please wait for the next update!</h3></center>
<hr>
{# <form action="" method="post" enctype="multipart/form-data">
    <!-- <div class="row"> -->
    <div class="col-12">
        {% if response != null %}
            <div class="alert alert-warning" role="alert">{{response}}</div>
        {% endif %}
        <div class="text-center">
        <p><b>NOTE:</b> This action is ireversible! If you click the button down bellow 
        this category will be ereased forever! The posts that are linked to this will not </p>
        </div>
        <br>
        <div class="text-center">
            <button class="btn btn-danger btn-lg" name="submit">Delete category</button>
        </div>
</form> #}
</div>
</div>
{% endblock %}