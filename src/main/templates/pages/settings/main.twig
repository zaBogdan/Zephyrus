{% extends 'layouts/settings.twig' %}

{% block body %}
<div class="page-header header-filter" data-parallax="true"></div>
<div class="main main-raised">  
    <div class="section">
        <div class="ml-auto mr-auto col-md-6">
            {% block alerts %}
            {% endblock %}
        </div>
        <div class="container texthandler">
            <div class="row">
                <div class="col-lg-3">
                    <ul class="nav flex-column nav-pills nav-pills-primary">
                        <li class="nav-item">
                            <a class="nav-link {{body.name=='edit-profile' ? 'active': null}}" href="/settings?page=edit-profile">Edit profile</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {{body.name=='change-password' ? 'active': null}}" href="/settings?page=change-password">Change password</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {{body.name=='change-email' ? 'active': null}}" href="/settings?page=change-email">Change Email</a>
                        </li>
                        {# <li class="nav-item">
                            <a class="nav-link {{body.name=='email-preferences' ? 'active': null}}" href="/settings?page=email-preferences" readonly>Email Preferences</a>
                        </li> #}
                    </ul>
                </div>
                <div class="col-lg-9">
                    {% block settings %}
                    {% endblock %}
                </div>
            </div>
        <!-- Aici trebuie puse setarile -->
        </div>
    </div>
</div>
{% endblock %}